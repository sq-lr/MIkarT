# Scenes

Empty on purpose. Unity scene files (`.unity`) require the Editor's asset
database to allocate GUIDs correctly, so none are hand-authored in this
bootstrap. Follow the one-time setup checklist in `docs/development.md` to
create `Main.unity` and wire up the GameObjects.
